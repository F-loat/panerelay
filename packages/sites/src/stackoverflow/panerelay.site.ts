import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'stackoverflow',
  name: 'Stack Overflow',
  version: '0.8.0',
  origins: ['https://api.stackexchange.com'],
  description: 'Public Stack Exchange questions, answers, tags, and user search.',
});
