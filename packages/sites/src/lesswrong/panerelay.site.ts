import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'lesswrong',
  name: 'LessWrong',
  version: '0.8.0',
  origins: ['https://www.lesswrong.com'],
  description: 'Public LessWrong posts, tags, comments, and user profiles through GraphQL.',
});
