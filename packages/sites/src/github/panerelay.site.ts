import { defineSite } from '@panerelay/site-kit';
export default defineSite({
  id: 'github',
  name: 'GitHub',
  version: '0.8.0',
  origins: ['https://github.com'],
  description: 'Cookie-backed GitHub account identity.',
});
