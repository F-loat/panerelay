import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'oeis',
  name: 'OEIS',
  version: '0.8.0',
  origins: ['https://oeis.org'],
  description: 'Public integer sequence search and metadata.',
});
