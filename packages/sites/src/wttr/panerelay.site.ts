import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'wttr',
  name: 'wttr.in',
  version: '0.8.0',
  origins: ['https://wttr.in'],
  description: 'Public global weather lookup and forecast.',
});
