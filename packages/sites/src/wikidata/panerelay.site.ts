import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'wikidata',
  name: 'Wikidata',
  version: '0.8.0',
  origins: ['https://www.wikidata.org'],
  description: 'Public Wikidata entity search and metadata.',
});
