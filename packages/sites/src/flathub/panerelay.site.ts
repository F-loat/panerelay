import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'flathub',
  name: 'Flathub',
  version: '0.8.0',
  origins: ['https://flathub.org'],
  description: 'Public Flatpak app search and AppStream metadata.',
});
