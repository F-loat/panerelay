import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'osv',
  name: 'OSV.dev',
  version: '0.8.0',
  origins: ['https://api.osv.dev'],
  description: 'Public open-source vulnerability queries and details.',
});
