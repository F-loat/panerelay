import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'reuters',
  name: 'Reuters',
  version: '0.8.0',
  origins: ['https://www.reuters.com'],
  description: 'Cookie-backed Reuters search and inline article extraction.',
});
