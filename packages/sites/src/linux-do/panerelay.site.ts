import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'linux-do',
  name: 'Linux.do',
  version: '0.8.0',
  origins: ['https://linux.do'],
  description: 'Cookie-backed Linux.do categories, tags, topics, search, and user activity.',
});
