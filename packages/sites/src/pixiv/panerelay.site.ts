import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'pixiv',
  name: 'Pixiv',
  version: '0.8.0',
  origins: ['https://www.pixiv.net'],
  description: 'Cookie-backed Pixiv illustration details, artists, search, and rankings.',
});
