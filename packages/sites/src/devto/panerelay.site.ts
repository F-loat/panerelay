import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'devto',
  name: 'DEV.to',
  version: '0.8.0',
  origins: ['https://dev.to'],
  description: 'Public developer articles and article metadata.',
});
