import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'nowcoder',
  name: 'NowCoder',
  version: '0.8.0',
  origins: ['https://gw-c.nowcoder.com', 'https://www.nowcoder.com'],
  description: 'NowCoder rankings, feeds, search, careers, messages, and interview preparation.',
});
