import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'tvmaze',
  name: 'TVmaze',
  version: '0.8.0',
  origins: ['https://api.tvmaze.com'],
  description: 'Public TV show search and metadata from TVmaze.',
});
