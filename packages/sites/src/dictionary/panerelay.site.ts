import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'dictionary',
  name: 'Dictionary',
  version: '0.8.0',
  origins: ['https://api.dictionaryapi.dev'],
  description: 'Public English definitions, synonyms, and examples.',
});
