import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'rubygems',
  name: 'RubyGems.org',
  version: '0.8.0',
  origins: ['https://rubygems.org'],
  description: 'Public RubyGems metadata and search.',
});
