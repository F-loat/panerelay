import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'wikipedia',
  name: 'Wikipedia',
  version: '0.8.0',
  origins: ['https://en.wikipedia.org'],
  description: 'Public Wikipedia search, article, random, and trending commands.',
});
