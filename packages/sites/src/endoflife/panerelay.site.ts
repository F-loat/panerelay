import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'endoflife',
  name: 'endoflife.date',
  version: '0.8.0',
  origins: ['https://endoflife.date'],
  description: 'Public product release cycles and end-of-life dates.',
});
