import { defineSite } from '@panerelay/site-kit';
export default defineSite({
  id: 'bloomberg',
  name: 'Bloomberg',
  version: '0.8.0',
  origins: ['https://feeds.bloomberg.com'],
  description: 'Public Bloomberg RSS feed aliases and crypto stories.',
});
