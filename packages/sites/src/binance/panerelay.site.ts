import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'binance',
  name: 'Binance',
  version: '0.8.0',
  origins: ['https://data-api.binance.vision'],
  description: 'Public Binance market data.',
});
