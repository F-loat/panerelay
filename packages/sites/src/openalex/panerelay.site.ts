import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'openalex',
  name: 'OpenAlex',
  version: '0.8.0',
  origins: ['https://api.openalex.org'],
  description: 'Public scholarly works search and metadata.',
});
