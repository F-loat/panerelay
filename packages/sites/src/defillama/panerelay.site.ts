import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'defillama',
  name: 'DefiLlama',
  version: '0.8.0',
  origins: ['https://api.llama.fi'],
  description: 'Public DeFi protocol TVL and metadata.',
});
