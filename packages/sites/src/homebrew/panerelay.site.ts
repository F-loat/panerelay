import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'homebrew',
  name: 'Homebrew',
  version: '0.8.0',
  origins: ['https://formulae.brew.sh'],
  description: 'Public Homebrew formula, cask, and analytics metadata.',
});
