import { defineSite } from '@panerelay/site-kit';

export default defineSite({
  id: 'artifact-upload-fixture',
  name: 'Artifact upload fixture',
  version: '1.0.0',
  origins: [],
  description: 'Daily-browser fixture for one multipart artifact.',
});
