## 1. Homepage content and interaction

- [x] 1.1 Replace the Setup automation handoff chooser and manual per-engine command with a bilingual Agent-facing Fetch usage guide and copyable example prompt.
- [x] 1.2 Remove handoff-only JavaScript and CSS while preserving the generic localized copy interaction and responsive Setup layout.

## 2. Validation and cleanup

- [x] 2.1 Update website source regressions for the Fetch guide, removed handoff UI, and retained raw CLI Fetch workflow.
- [x] 2.2 Run website tests, formatting or type checks, strict OpenSpec validation, and `git diff --check`.
- [x] 2.3 Verify the English and Chinese Setup section in daily Chrome at desktop and 375-pixel widths, including copy behavior and horizontal overflow; confirm that no compatibility documentation update is required.
